using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestionResult : Result<List<Question>>
{
    public QuestionResult(string name, List<Question> data) : base(name, data) {}
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
